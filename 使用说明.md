# dsh-topic-trail — 会话工作线索悬浮窗

> 用 dsh 写东西想到哪写到哪，写完回头一看——刚才到底干了啥？这个插件就是解决这个问题的。

给 DeepSeek Harness（dsh）的 Web UI 加一个**可拖动的悬浮窗**，实时把你和 AI 正在做的工作提炼成「话题（线索）」和「进度步骤」。点开话题能看到一步一步都干了什么，跨会话、跨工作区的工作脉络一目了然。

总结走 dsh 已配置的 DeepSeek 路由（`ctx.llm`），**不需要额外配 API key**。

**谁适合用**：多会话并行开发的人、长对话经常找不到之前决策的人、写完就忘自己干了啥的人、想给室友/同事分享工作进度的人。

---

## 30 秒快速上手

1. 安装插件（见下方「安装」），重启 dsh
2. 左上角出现蓝色小球（工作线索悬浮窗收起态），开始和 AI 对话
3. 点小球展开面板，对话过程中线索自动生成
4. 点话题看步骤，点步骤跳转到对应对话位置

---

## 解决的痛点

| 痛点 | 插件怎么解决 |
|---|---|
| 想到哪写到哪，写完不知道自己干了啥 | 实时把会话活动提炼成话题+步骤，随时回看 |
| 长对话里找不到之前的某个决策 | 步骤按时间排列，**点击步骤直接跳转到对应对话位置**并自动滚动 |
| 多会话切换后忘记之前的进度 | 三级线索网络：全部工作区 → 工作区 → 会话，相似话题自动合并 |
| 工作区之间的工作脉络看不清 | 工作区级线索由下属会话的相似话题合并而成，可展开下钻到每个来源会话 |
| 手动整理太麻烦 | 右键「重新生成线索」一键总结；首次打开自动导入所有未归档会话 |
| 线索重复/拆分不合理 | 同一对话范围内**拖拽合并**，AI 自动找共同点、整理步骤和时间关系 |
| 子代理后台任务污染线索 | 自动识别并过滤 `origin: 'subagent'` 的子代理会话，不记录其活动 |
| LLM 总结偶发失败 | 三级容错解析（围栏→包夹→截断修复），失败静默回退规则模式 |

---

## 特色功能

### 悬浮窗与交互
- **可拖动小球**：收起为圆形小球后可拖动，位置记忆在 localStorage；展开后面板左上角固定在小球右侧
- **呼吸圆环**：小球带半透明呼吸辉光，任务进行时呼吸加快
- **眼球动画**（可选，默认关）：小球数字变瞳孔，执行任务时视线四处飘，空闲时跟随鼠标，偶尔眨眼
- **悬停展开**：每个线索默认只显示标题，鼠标移上去后描述平滑展开，点击才展开步骤列表
- **展开/收起过渡**：点小球时面板淡入、小球淡出；收起时反向，不再是瞬切
- **线索排序**：会话里的线索按**最近修改时间倒序**（刚更新的排最上，长时间没动的沉底）；工作区视图的分组也按组内最新活动排序
- **点击子线索下钻**：工作区视图里点开一组线索后，点其中某条会切到它的会话**并展开这条线索**（以前只切会话、不展开）
- **面板可缩放**：右下角拖拽调整宽高，宽度缩小时字体等比缩小（不超过原始大小），高度调整上限
- **仿 dsh 深色 UI**：毛玻璃面板、圆角卡片、用户/助手/工具/推理四色步骤侧条，和 dsh 原生风格一致

### 三级线索网络
- **全部工作区**：跨工作区的相似线索合并到收起栏
- **工作区**：该工作区下所有会话的相似话题自动合并，可展开下钻到来源会话
- **会话**：原始话题 + 步骤明细
- 鼠标悬停任意线索，tooltip 显示它来自哪些工作区/会话（多来源会逐条列出）
- **工作区置顶**：右键工作区可置顶，置顶工作区始终排在前面
- **3 天未动隐藏**：超过 3 天无活动的线索自动收起，滚轮底部有「展开更早的线索」按钮
  - 若整个会话的线索都超过 3 天（点开一个很久没动的对话），默认显示**最新 5 条**、按钮变为「展开全部线索（N）」，不让面板空着
- **导航列表只收有意义的会话**：创建后从未对话过的空会话（无标题、无线索，dsh 也不会给它起标题）不进入下拉，避免一排「未命名 xxxxxx」占位；它一旦有了线索就会自动出现。子代理会话（header `origin: 'subagent'`）同样不出现

### AI 总结与合并
- **右键重新生成线索**：右键全部工作区/工作区/会话/线索卡片，触发 LLM 重新总结（全面覆盖历史所有消息）
- **拖拽合并**：同一对话范围内把一条线索拖到另一条上，AI 找共同点、合并步骤并整理时间顺序；合并后锁定（🔒），不会被自动总结覆盖
- **从修改中学习**：你手动改过的线索会记录下来，下次 LLM 总结时参考你的偏好
- **工作区结构记忆**：每个工作区首次全量扫描后保存结构画像和总结规律，后续只增量更新，省 token
- **话题演化阶段**：阶段固定不删，完成当前阶段或主动跳转后才生成新阶段
- **展开先看历程**：点开一条线索时优先显示**历程**（阶段里程碑），步骤收在「显示步骤（N 步）」按钮后面——避免上千步的流水账把脉络淹没。没有 AI 历程的线索会用步骤推断一个基础历程（起点 / 遇到过失败 / 最近进展），所以每条线索展开都有脉络可看
  - 卡片不会被 flex 压扁（`flex-shrink: 0`），所以线索很多的会话里展开区同样完整可见
- **点击跳转定位**：点线索/步骤/里程碑会打开对话并滚动到大致位置
  - 位置优先按**消息时间**精确匹配（会话事件自带真实时间）；匹配不到时按「会话内事件序号的相对百分比」估算（`(seq - 最小seq) / (最大seq - 最小seq)`）
  - 打开对话本身很快（异步加载消息），定位会在消息渲染出来后自动补上
  - 早期版本生成的旧线索时间不可信（都挤在导入那一刻），想让它也精确可以右键会话 →「重新生成线索」
- **重新生成线索**：右键会话/工作区 →「重新生成线索」会丢弃现有线索、从完整事件日志重建后再交给 AI 总结
  - 失败保护：读不到事件日志时不会清空；结果为空或缩水到不足原来 1/3 时自动回滚
  - 保留手动成果：**已锁定**和**手动合并**过的线索不会被冲掉，重建后会重新编号放回
- **收起行为**：浮层都支持点击外部关闭，Esc 逐层关闭（右键菜单 → 画笔菜单 → 导航下拉 → 搜索 → 便签 → 展开的线索）
  - 卡片的摘要/元信息区随鼠标悬停出现、移开收起（点过之后也会立即释放焦点，不会一直挂着）
  - 点击展开的历程/步骤区会保持展开，再点一次卡片标题、按 Esc、或切换对话才会收起
  - **展开面板后 0.6 秒内又收起**（只是瞄一眼）也会自动冒一条气泡——不用为了看气泡专门去连点小球
- **小球大小**：设置里可调（60%~160%），**悬浮球与面板头部那颗球同步缩放**
  - 头部球放大时文字区会自动让位（`padding-left` 跟着加宽），球的圆心保持不动，不会往右下角漂
- **插话间隔**：小球多久冒一次气泡，设置里可调（**5~120 秒，默认 30 秒**）
  - 实际间隔会有 0.7~1.5 倍随机抖动（默认约 21~45 秒一次），不会像定时器一样机械
  - 觉得话太多就把滑块往右拉；想让它活跃一点就调短
- **已删除的工作区 / 归档的对话**：在 dsh 里删掉的工作区、归档掉的对话，会从这里一并隐藏（每几秒同步一次 dsh 的分组数据）
  - 设置里打开「展示已删除的工作区」可以把它们补回列表，工作区名会标注「已删除」
  - 过滤很保守：一条线索只有在它的**所有来源会话都已消失**时才隐藏；读不到 dsh 分组数据时干脆不过滤，宁可多显示也不清空面板
- **状态点的三档颜色**：线索前的圆点不只看「进行中/已完成」，还会参考**最后活动时间**——避免停手不做的线索一直亮着绿点
  - 🟢 **亮绿 + 脉冲**：10 分钟内有活动（真的在做）
  - 🟡 **暗绿（无脉冲）**：10 分钟 ~ 1 天没动（暂时搁置）
  - ⚪ **灰**：超过 1 天没动（实际已停），或已完成
  - 鼠标悬停会说明当前是哪种状态；**只影响显示，不改动数据**

### 笔记与搜索
- **每对话独立笔记**：头部笔记按钮召唤浮窗，支持 Markdown 实时编辑（不区分预览/编辑），每个对话一份
- **笔记画笔**：笔记浮窗右键菜单含画笔/橡皮，可选 7 种颜色与 4 档笔触粗细（橡皮自动加粗），默认 DeepSeek 蓝
- **全局搜索**：笔记按钮旁的 🔍 图标，在当前视图范围（全部/工作区/会话）内搜索线索

### 皮肤与主动展示
- **双皮肤**：默认皮肤（经典蓝球）和 DeepSeek 娘皮肤（小鲸鱼形象 + 偏移配色），设置中切换
- **主动展示气泡**（默认皮肤默认开，DeepSeek 娘默认开）：每隔波动时间小球冒出聊天气泡，内容融合最近线索 + AI 随机生成的评论/指导/玩笑；DeepSeek 娘模式穿插趣味话术
- **导出工作区**：右键工作区可导出该工作区所有线索为 JSON

### 跳转与定位
- **步骤点击跳转**：点开话题后点任意步骤，自动打开对应对话并滚动到该步骤发生的位置（50ms 立即检查 + 100ms 复查 + 1.5s 轮询兜底）；跳转期间该步骤卡片会呼吸高亮并显示「打开中…」
- **双击线索/对话跳转**：工作区视图中双击对话或其线索，跳转到对应对话（不触发 dsh 原生双击切换）

### 设置与性能
- **设置页集成**：dsh 设置里新增「任务线索」栏，可开关插件、切换对话时自动跟随、开关学习功能、调整总结方式、切换刷新间隔（1s/3s/5s/10s 预设）、切换皮肤、开关眼球动画、开关主动展示、开关工作区记忆
- **设置即时生效**：设置保存后立即作用于前端（皮肤/轮询/动画）与后端（总结方式/导入），不需要重启；刷新页面也会用上次的设置值渲染，不会退回默认
- **首次自动导入**：第一次打开时后台慢慢加载所有未归档会话的线索，不截断线程
- **性能优化**：服务端快照缓存 + 前端 localStorage 秒开 + version 跳过重渲染 + 自适应轮询（以 pollMs 为基线：页面隐藏 1s 空转/折叠 ×6/展开忙 ×0.8/展开闲 ×2），不卡界面
- **轻索引 + 步骤按需加载**：快照只带线索元数据与步骤数（`stepCount`），不搬运步骤正文；展开线索时才按页取。真实数据（45 会话 / 15816 步）实测每轮轮询 **4354.9 KB → 112.5 KB（↓97.4%）**
- **强制不删话题**：默认开启，防止 LLM 总结时意外丢失已有话题

---

## 安装

### 方法一：dsh plugin 命令（推荐）

```powershell
# 指向你自己的 dsh 安装目录（下面用 $DSH_HOME 表示）
$env:PATH = "$DSH_HOME\tools\node_modules\.bin;" + $env:PATH
dsh plugin --profile web add file:C:/path/to/dsh-topic-trail
```

然后重启 dsh。

### 方法二：丢给你的 AI 助手

把这个仓库地址发给你的 AI 助手（豆包、Claude、ChatGPT 等），说：

> "帮我把 dsh-topic-trail 插件安装到我的 dsh 里，插件目录在 harness-v2/plugins/ 下，安装副本在 harness-v2/profiles/web/node_modules/ 下。"

AI 助手会帮你完成下载、复制、同步和重启。

### 验证

```powershell
dsh --profile web --dump-config | Select-String -Pattern "topic-trail"
```

---

## 使用方法

### 基本操作
1. 打开 dsh Web UI，左上角出现蓝色小球（工作线索收起态）
2. 点小球展开面板；点小球（展开态）可收起
3. 头部下拉选择查看范围：**全部工作区** / 某个**工作区** / 某个**会话**
4. 鼠标悬停线索卡片查看描述；点击卡片展开步骤列表
5. 点任意步骤 → 自动跳转到对应对话并滚动到发生位置
6. 面板右下角拖拽可调整宽高

### 右键菜单
- 右键导航里的工作区 →「重新生成线索」「置顶/取消置顶」「导出工作区」
- 右键导航里的会话 →「重新生成该对话的所有线索」
- 右键线索卡片 →「重新生成线索」「编辑此线索」「删除此线索」
- 右键笔记浮窗 →「画笔」「橡皮」（切换标注工具）

### 拖拽合并
1. 确保在**会话级**视图（工作区级不支持拖拽合并）
2. 按住一条线索拖到另一条线索上
3. AI 自动分析两条线索的共同点，合并步骤并整理时间顺序
4. 合并后的线索带 🔒 标记，不会被后续自动总结覆盖

### 笔记
1. 面板头部点笔记图标（📝）召唤笔记浮窗
2. 直接在浮窗中编辑 Markdown，实时渲染
3. 右键浮窗空白处切换画笔/橡皮做标注；菜单里可选颜色与笔触粗细（4 档）
4. 每个对话独立一份笔记，切换对话自动加载对应笔记

### 搜索
1. 面板头部点 🔍 图标，工作区选择处变为搜索输入框
2. 输入关键词，在当前视图范围（全部/工作区/会话）内搜索线索
3. 再次点 🔍 退出搜索

### 设置
dsh 设置 → 「任务线索」栏：

| 选项 | 默认 | 说明 |
|---|---|---|
| 启用插件 | 开 | 全局开关，关闭后悬浮窗不显示 |
| 切换对话时跟随 | 开 | 切到新对话时自动把线索视图切到该对话 |
| 从修改中学习 | 开 | 记录你手动改的线索，下次总结时参考 |
| 总结方式 | auto | auto（规则+LLM）/ llm（纯LLM）/ rule（纯规则，零模型调用） |
| 刷新间隔 | 3000ms | 轮询基线，可选 1s/3s/5s/10s；展开忙 ×0.8 / 闲 ×2 / 收起 ×6，钳制 1s~120s |
| 皮肤 | default | default（经典蓝球）/ deepseek（DeepSeek 娘） |
| 眼球动画 | 关 | 小球数字变瞳孔，视线跟随/飘动，偶尔眨眼 |
| 主动展示 | 开（默认皮肤） | 小球定时冒出气泡，融合线索+AI话术 |
| 工作区结构记忆 | 开 | 每个工作区保存结构画像，增量更新省 token |
| 强制不删话题 | 开 | LLM 总结时保留已有话题，防止意外丢失 |

设置保存后**立即生效**（写入 `data/topic-trail/config.json` 并马上作用于事件处理与首启导入），不需要重启 dsh；前端会同时更新皮肤/轮询间隔等，刷新页面也用上次的值渲染。

> 若某项改了没反应，先在设置页再保存一次（会把当前生效配置整体写回），再检查 `data/topic-trail/config.json`。

---

## 项目结构

```
dsh-topic-trail/
├── package.json              # dsh.bundle.patch + dsh.client(web) 声明
├── cordis.patch.yml          # 插件挂载层 + 默认配置
├── 使用说明.md              # 本文件（GitHub 上的 README 也是它）
├── assets/
│   └── deepseek-avatar.png   # DeepSeek 娘完整立绘
├── lib/
│   ├── index.js              # Host 半：事件监听、LLM 总结、API 路由、持久化、工作区网络、子代理过滤
│   ├── client.js             # Client 半：shell.overlay 悬浮窗（零构建，ModuleLoader bundle）
│   ├── deepseek-avatar.png   # 完整立绘（?full=1 时返回）
│   └── deepseek-face.png     # 头部特写（默认返回；小球只有 30px，整张立绘缩下去会糊）
├── .preview-skin.mjs         # 开发用：抽真实 CSS 渲染小球/气泡预览图
├── .check-css.mjs            # 开发用：找 CSS 定义了但 DOM 没用的类（以及反向）
└── .check-unused.mjs         # 开发用：找从未被读取的 state/ref 和函数
```

> 换皮肤头像：替换 `lib/deepseek-face.png` 即可（路由带 ETag，浏览器会立刻拿到新版）。
> 想换回完整立绘可以在 `.dtt-fab-face` 的 CSS 里把 URL 改成 `/plugins/topic-trail/avatar?full=1`。

### 数据流

```
dsh 会话事件（user/assistant/tool/turn-end）
        │  ctx.on('session/event')
        │  └─ 子代理会话（origin:'subagent'）直接跳过
        ▼
Host 缓冲（每会话 ≤40 条，持久化到 data/topic-trail/<sessionId>.json）
        │  turn/end 后防抖 1.5s
        ▼
DeepSeek LLM（ctx.llm.stream + 话题总结提示词）
        │  输出 → 三级容错 JSON 解析（围栏→包夹→截断修复）
        │  失败 → 回退规则模式
        ▼
GET /plugins/topic-trail/snapshot  ──自适应轮询──►  Client 悬浮窗
```

### Host 侧 API

| 路由 | 方法 | 说明 |
|---|---|---|
| `/plugins/topic-trail/snapshot` | GET | 全部线索快照（**轻索引**：含 `stepCount`，不含 `steps`）+ 工作区线索网络 + version |
| `/plugins/topic-trail/steps` | GET | 按需取某条线索的步骤正文（`sessionId`/`topicId`/`offset`/`limit`，单页默认 200、上限 500） |
| `/plugins/topic-trail/sessions` | GET | 会话列表（含标题/是否有线索，已过滤子代理） |
| `/plugins/topic-trail/config` | GET/POST | 读取/更新运行时配置 |
| `/plugins/topic-trail/import` | POST | 导入旧会话，从日志重建话题 |
| `/plugins/topic-trail/summarize` | POST | 手动触发 LLM 总结（异步，支持单会话/多会话/工作区/全部） |
| `/plugins/topic-trail/merge` | POST | 拖拽合并两条线索（AI 找共同点） |
| `/plugins/topic-trail/notes` | GET/POST | 读取/保存某会话的笔记（Markdown + 画笔标注） |
| `/plugins/topic-trail/bubble-quotes` | GET | 获取主动展示气泡话术（LLM 生成 + 模板融合） |
| `/plugins/topic-trail/avatar` | GET | 皮肤头像（PNG）。默认给「头部特写」（小球尺寸用），`?full=1` 取完整立绘；带 `ETag`，换了图片文件浏览器立即生效 |
| `/plugins/topic-trail/export` | GET | 导出指定工作区的所有线索为 JSON |

---

## 更改与更新

### 修改源码后
`lib/` 是源码，pnpm 对 `file:` 依赖是**复制安装**，改完必须同步到已安装副本：

```powershell
Copy-Item lib\*.js ..\..\profiles\web\node_modules\dsh-topic-trail\lib\ -Force
```

- 改 `lib/index.js`（host）→ **重启 dsh** 生效
- 改 `lib/client.js`（前端）→ **浏览器刷新**即可

### 数据文件
运行时数据在 `harness-v2/data/topic-trail/`：
- `<sessionId>.json` — 每个会话的线索、步骤、缓冲、修改日志
- `<sessionId>.notes.md` — 每个会话的笔记内容
- `config.json` — 运行时配置（设置页改动持久化到这里）
- `workspace-profiles.json` — 工作区结构记忆（每个工作区的结构画像和总结规律）
- `llm-error.log` — LLM 总结失败日志

这些**不要提交到 git**（已在 .gitignore 排除）。

### 更新到新版本
```powershell
cd harness-v2
git -C plugins/dsh-topic-trail pull   # 如果是 git clone 的
# 或重新下载覆盖 lib/
Copy-Item plugins\dsh-topic-trail\lib\*.js profiles\web\node_modules\dsh-topic-trail\lib\ -Force
# 重启 dsh
```

---

## 配置（cordis.patch.yml）

| 配置项 | 默认 | 说明 |
|---|---|---|
| `summarize` | `auto` | auto：规则即时生成 + turn 结束后 LLM 总结替换；llm：纯 LLM；rule：纯规则零调用 |
| `provider` / `model` | 自动 | 留空自动探测 dsh 已配置的 DeepSeek 路由；可显式指定 |
| `pollMs` | 3000 | 前端轮询基线（毫秒）；展开忙 ×0.8 / 闲 ×2 / 收起 ×6 |
| `bufferSize` | 40 | 送入 LLM 总结的最近事件条数上限 |
| `followSession` | true | 切换对话时自动跟随 |
| `learnFromModifications` | true | 从手动修改中学习 |
| `enabled` | true | 插件全局开关 |
| `skin` | `default` | 皮肤：default / deepseek |
| `eyeAnimation` | false | 眼球动画开关 |
| `proactiveDisplay` | true | 主动展示气泡开关 |
| `workspaceMemory` | true | 工作区结构记忆开关 |
| `preserveTopics` | true | 强制不删话题 |
| `workspaceViewMode` | `bySession` | 工作区视图模式 |

---

## 常见问题

**Q：悬浮窗不显示？**
A：检查 dsh 设置 → 任务线索 → 「启用插件」是否打开；重启 dsh；查看控制台是否有 React 错误。

**Q：线索一直是空的？**
A：新会话需要等一轮对话结束（turn/end）后才会触发 LLM 总结；可以右键会话 →「重新生成线索」手动触发。

**Q：点击步骤跳转定位不准？**
A：dsh 用虚拟列表，目标消息可能不在 DOM 里。插件会先滚到底部触发加载，再按时间匹配（50ms 立即检查 + 100ms 复查 + 1.5s 轮询兜底），匹配不到时按步骤序号比例滚动，最后兜底底部。

**Q：拖拽合并不生效？**
A：拖拽合并仅在**会话级**视图可用，工作区级和全部工作区级不支持。确保你当前选中的是某个具体会话。

**Q：LLM 总结失败怎么办？**
A：检查 dsh 的 DeepSeek API key 是否有效（web 页面 → 模型设置）；失败会自动回退规则模式，错误详情写在 `data/topic-trail/llm-error.log`。新版已加三级 JSON 容错解析，截断/带杂质的输出也能救回大部分。

**Q：展开线索只看到 200 步？**
A：步骤是按需分页加载的（快照不携带步骤正文）。点列表末尾的「加载更多」继续取下一页。

**Q：改了「总结方式」要重启才生效吗？**
A：不需要。运行时配置在插件装载阶段同步读取，设置页保存后立即作用于事件处理与首启导入。

**Q：子代理的任务也被记成线索了？**
A：已修复。插件只按 session header 的 `origin: 'subagent'`（或「有 parentSession 且 delegationDepth > 0」）判定子代理，全链路过滤（事件/缓存/快照/会话列表/工作区），子代理活动不会产生线索。
注意**不要**用 `agentPreset` 判定——dsh 里每个会话（含顶层主会话）都有这个字段，拿它当子代理标记会把主会话全过滤掉。

**Q：工作区里显示的对话数比实际多，或者同一路径出现好几个工作区？**
A：已修复。会话标题/cwd 缓存现在统一按规范 id 存储，工作区按真实工作目录（cwd）分组：同一路径只算一个工作区，同一对话只算一次。修复前一个对话会被算成 3 个（`session-x` / `x` / `session-x` 三种写法各一份）。

**Q：点开「选择旧对话」下拉时卡很久（十几秒）？**
A：已修复。旧版会在响应前同步折叠全部会话的标题（本机实测 69 个会话要 31 秒）。现在：
- `/sessions` 与 `/snapshot` **都不阻塞响应**（首屏 ~0.1s / ~5s），两个请求还会合并成一次刷新；
- 标题折叠改成分块后台推进（每块 8 个会话），一批批写进缓存，界面上标题会逐步出现；
- 折叠带进行中去重：重复请求不会在宿主里排队（旧实现因此永远读不完标题）。

**Q：刚打开时对话列表显示「未命名会话」？**
A：正常，等等就好。标题要读会话日志折叠，宿主内部 4 并发 + 每个会话全量校验，本机 56 个会话约 1 分钟读完，期间标题分批出现（实测 24s→10 个、50s→40 个、58s→47 个）。读不到的会话本身就没有标题事件，会一直显示「未命名会话」。

**Q：配置项写了没效果（比如 `preserveTopics: false`）？**
A：已修复。`workspaceViewMode` / `eyeAnimation` / `preserveTopics` / `workspaceMemory` / `skin` / `proactiveDisplay` 过去被代码硬编码，改 `cordis.patch.yml` 没用。现在 patch 里的值会被读取，设置页保存的值（`data/topic-trail/config.json`）优先级更高、立即生效。

**Q：右下角出现「⚠ 工作线索出错了」怎么办？**
A：先按提示刷新页面。若**刷新后仍然出现**，那就是代码问题，请把控制台里 `[dsh-topic-trail] render error` 后面的堆栈反馈出来。插件自带错误边界：面板渲染出错时只降级成这条提示，不会连线索数据一起消失。

**Q：DeepSeek 娘皮肤头像不显示？**
A：确认 `lib/deepseek-face.png` 存在且已同步到安装副本；头像通过 `/plugins/topic-trail/avatar` 路由提供，不走静态文件服务。

**Q：主动展示气泡好久不冒？**
A：气泡间隔是波动的：收起时随机 8~18 秒一个（面板展开时不计时），且页面隐藏时暂停。话术会走 AI 生成（融合最近线索，每次要 5 条），缓存用完后会异步补货，拿不到时回退内置模板。DeepSeek 娘模式有额外趣味话术池。可在设置中关闭/开启。

**Q：改了「刷新间隔」（pollMs）没反应？**
A：现在会生效。设置页里有 1s / 3s / 5s / 10s 档位，点一下即保存（立即重建轮询定时器）；轮询周期以它为基线：展开且忙时 ×0.8、闲时 ×2、收起时 ×6（钳制 1s~120s）。想用别的值可直接改 `data/topic-trail/config.json` 的 `pollMs`（800~60000）。

**Q：笔记画笔的粗细在哪调？**
A：打开笔记浮窗 → 右键浮窗空白处 → 菜单里有「颜色」（7 色）和「粗细」（4 档，圆点大小即笔触粗细）。橡皮会自动按 ×4 加粗。

**Q：换了皮肤头像的图片文件，界面没变？**
A：现在会自动更新。`/plugins/topic-trail/avatar` 带 `ETag` + `Cache-Control: no-cache`，文件一变浏览器立刻拿到新图（旧版本是 24 小时强缓存，换了图也看不到）。默认返回 `lib/deepseek-face.png`（头部特写）；完整立绘需要 `?full=1`。

**Q：面板停在旧数据不动了？**
A：现在会明确提示。刷新请求带 15 秒超时，并发重试会自动跳过（不会把请求堆起来）；失败时面板顶部出现红色「⚠ 刷新失败，显示的是上次的数据」，点一下即可重试。过去这种失败是完全静默的——`setError` 只存不显示，面板停更时看不出来。

---

## 说明与限制

- 话题/步骤 id 基于会话事件序号生成，LLM 重总结后 id 保持稳定，前端 diff 平滑
- LLM 总结失败（未配置模型、网络错误、输出非法 JSON）静默回退规则模式；JSON 解析采用三级容错（代码围栏 → 严格包夹 → 截断修复+悬空逗号清理）
- 子代理会话按 header 血缘判定（`origin: 'subagent'`，或 `parentSession` + `delegationDepth > 0`）全链路过滤；`agentPreset` 是每个会话都有的 preset id，不作为子代理依据
- 会话 id 对外统一用规范形式（去掉 `session-` 前缀）：同一对话的 `session-x` 与 `x` 两种写法被视为同一个，缓存、工作区计数、会话列表都只算一次
- 工作区按会话的真实工作目录（cwd）分组：同一路径只算一个工作区；标题与 cwd 缓存在 snapshot 与 /sessions 之间共用，30 秒后台刷新一次，不阻塞请求（两个请求会合并成一次刷新）
- 请求路径上没有同步文件系统调用：会话目录 mtime 由后台并行预读（并发 4）进缓存，快照只读缓存
- 会话标题折叠（宿主内部 4 并发 + 每会话全量校验，最贵的活）分块后台推进，每块 8 个会话；带进行中去重与 5 分钟结果缓存，同一批不会重复读
- 传给宿主的会话 id 必须是原始形式（`session-<uuid>`）：`readTitleSnapshots` 认不出规范化后的裸 uuid，会返回 `SESSION_QUERY_SESSION_NOT_FOUND`。对外展示与缓存键用规范形式，调宿主 API 用原始形式
- 前端刷新带 15 秒超时与并发抑制：上一次没返回就跳过这一拍；snapshot 与 sessions 各自兜错，单边失败保留上次数据并在面板顶部提示，不会静默停更
- 轮询周期以 `pollMs` 为基线（展开忙 ×0.8 / 闲 ×2 / 收起 ×6，钳制在 1s~120s）；旧版把它写死成 2s/5s/15s，设置项形同虚设
- 主动展示气泡的话术由 `/bubble-quotes` 生成（5 条一批），缓存空时异步补货、拿不到回退内置模板；旧版 `fetchQuotes` 定义了却从未被调用，气泡永远只有模板
- 「重新生成线索」（forceRegenerate）会先备份现有话题：LLM 不可用导致重建失败时自动还原，不会把线索清空
- LLM 路由解析里 provider 与 model 各自独立探测：只配了 provider 时自动取该 provider 的首个模型，而不是直接报错
- 步骤清洗逻辑统一在 `cleanLlmSteps()`（LLM 总结与合并共用），差异用参数表达（seq 兜底策略、title/detail 回退、占位文案）
- `cordis.patch.yml` 里的 `summarize` / `pollMs` / `bufferSize` / `provider` / `model` / `followSession` / `learnFromModifications` / `enabled` / `eyeAnimation` / `preserveTopics` / `workspaceMemory` / `workspaceViewMode` / `skin` / `proactiveDisplay` 都会被读取；设置页保存的值写入 `data/topic-trail/config.json` 并覆盖这些基线值
- 工作区线索按标题相似度合并（bigram Jaccard 聚类）；dsh 的工作区分组从 `localStorage['dsh.workspace.view.v5']` 读取，和 dsh 界面一致
- 合并后的线索锁定（🔒），防止被自动总结覆盖
- 快照是「轻索引」：只带线索元数据 + 步骤数；步骤正文由 `/plugins/topic-trail/steps` 按需分页取（单页默认 200、上限 500），前端悬停线索时会预取首页
- 设置页的改动立即生效：运行时配置（`data/topic-trail/config.json`）在 apply() 阶段同步读取，生效配置以同一对象引用就地刷新
- 眼球动画、主动展示、皮肤等视觉功能可在设置中独立开关
- 面板宽高可拖拽调整，宽度缩小时字体等比缩小（上限为原始大小），过窄时隐藏「任务线索」标题给搜索栏腾空间
- 中英文界面适配：设置项和 UI 文案支持中英文
- 皮肤头像默认返回「头部特写」`lib/deepseek-face.png`（小球只有 30px）；完整立绘走 `?full=1`。换图后浏览器凭 ETag 立即更新，不需要改版本号
- 设置值有 localStorage 缓存：刷新页面首帧就用上次的值渲染（不会先闪一下默认皮肤），随后与服务器对齐
- 被动展示的气泡里会显示关联线索（`bubble.topic`），点气泡可跳到对应会话
- 笔记画笔的颜色与粗细在笔记浮窗的右键菜单里选（4 档笔触，橡皮自动 ×4）

---

## 📌 文档维护提醒

**每次新增/修改功能后，请同步更新本说明**，重点检查：

- [ ] 「特色功能」是否新增了条目
- [ ] 「使用方法」是否有新操作需要说明
- [ ] 「Host 侧 API」表格是否有新增/变更路由
- [ ] 「配置」表格是否有新增配置项
- [ ] 「常见问题」是否需要补充
- [ ] 「项目结构」是否有新增文件

模型迭代快，文档和代码不同步会让使用者困惑。改完代码顺手改本说明，保持一致。
